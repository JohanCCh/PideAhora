import React from 'react';
import {Text, View} from 'react-native';
import {styles} from '../theme/app-theme';

export const BillingScreen = () => {
  return (
    <View style={styles.container}>
      <Text style={styles.titleHeader}> Facturación </Text>
    </View>
  );
};
